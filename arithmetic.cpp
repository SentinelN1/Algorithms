#include <iostream>
#include <stack>

int main() {
    std::string str;
    std::stack<char> stack;
    std::getline(std::cin, str);
    for (char c : str) {

    }
    std::cout << str;
    return 0;
}
